# .github

Organization-wide defaults for [scm-bench](https://github.com/scm-bench): the profile README, brand assets, the shared label set, and the reusable CI/release workflows every bench repository calls into.
